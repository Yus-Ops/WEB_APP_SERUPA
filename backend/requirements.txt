# requirements.txt — HuggingFace Space (CPU). Satu paket per baris (format pip valid).
# Versi mengikuti env lokal torch_env yang sudah teruji; HANYA torch yang diganti ke
# CPU — build +cu126 tidak ada di PyPI & terlalu besar untuk Space gratis (CPU-only).
# pandas sengaja dibuang: tidak dipakai app.py saat runtime (hanya indexer.py).
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0+cpu
numpy==2.4.3
scikit-learn==1.8.0
transformers==5.5.4
sentence-transformers==5.6.0
umap-learn==0.5.12
fastapi==0.138.0
uvicorn[standard]==0.49.0
supabase==2.31.0
python-dotenv==1.2.2
pydantic==2.13.4
